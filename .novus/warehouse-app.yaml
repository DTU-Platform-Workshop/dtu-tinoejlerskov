apiVersion: novus.legogroup.io/v1alpha1
kind: WebApp
metadata:
  name: warehouse
  labels:
    novus.legogroup.io/app-id: warehouse-app
spec:
  ingress:
    subdomain: warehouse
  service:
    image:
      # Make sure to update these values:
      repository: ghcr.io/dtu-platform-workshop/dtu-tinoejlerskov/warehouse
      tag: 20260921-6c04837

    useSamePortForHealthProbes: true

    resources:
      limits:
        cpu: 100m
        memory: 128Mi
      requests:
        cpu: 50m
        memory: 64Mi
